apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    // Uncomment the next line to add compile-time dependencies to the main source set
    // compile()
    testCompile('junit:junit:4.12')
}